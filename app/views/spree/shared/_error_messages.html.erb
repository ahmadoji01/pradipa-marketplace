<% if target && target.errors.any? %>
    <div class="alert alert-danger" role="alert" data-hook>
        <p><%= t('spree.there_were_problems_with_the_following_fields') %>:</p>
        <ul>
            <% target.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
        </ul>
    </div>
<% end %>