<% if adjustments.sum(&:amount) != 0 %>
  <tr>
    <th><%= type %>: <%= label %></th>
    <td>
      <span class="amount"><%= Spree::Money.new(adjustments.sum(&:amount), currency: @order.currency) %></span>
    </td>
  </tr>
<% end %>
